services:
  brewos-cloud:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - brewos-data:/data
      - ../web/dist:/app/web:ro
    environment:
      - PORT=3001
      - DATA_DIR=/data
      - WEB_DIST_PATH=/app/web
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
    restart: unless-stopped

volumes:
  brewos-data:

