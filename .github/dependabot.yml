# Dependabot configuration
# Keeps dependencies up to date automatically
# Documentation: https://docs.github.com/en/code-security/dependabot

version: 2
updates:
  # Note: This repository doesn't use GitHub Actions workflows,
  # so the github-actions ecosystem is not included.
  # If workflows are added in the future, add:
  # - package-ecosystem: "github-actions"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"

